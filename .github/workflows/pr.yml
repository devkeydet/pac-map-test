name: pr
on:
  pull_request:
    branches:
    - main
jobs:
  pr:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: |
          dotnet build plugins\pacmaptest.plugins -c release



